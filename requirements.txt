# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1
apscheduler==3.10.4
python-multipart==0.0.26

# Wyoming protocol (Home Assistant STT integration)
wyoming>=1.5.3

# ONNX model inference (DTLN noise suppressor, SileroVAD, WeSpeaker speaker ID)
onnxruntime>=1.18.0

# SileroVAD — ONNX model is bundled inside the package (no runtime download needed)
silero-vad>=5.1

# Speaker identification (WeSpeaker ECAPA-TDNN — ONNX backend, no torch)
wespeakerruntime>=0.0.4

# Audio I/O (reference clip loading, temp files for speaker embedding)
soundfile>=0.12.1
numpy==2.2.1

# HTTP clients — aiohttp for HA webhook, httpx for remote STT transcriptions endpoint
aiohttp>=3.9.0
httpx>=0.27.0
